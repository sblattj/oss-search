[package]
name = "oss-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
oss-core = { path = "../oss-core" }
oss-live = { path = "../oss-live" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
oss-core = { path = "../oss-core" }
